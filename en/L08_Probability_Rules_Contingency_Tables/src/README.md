# L08 incremental programs

Run the three numbered scripts in order. Each is standalone, reproduces the same four-cell table, and writes one figure to ../figures/. The programs force the Agg backend to avoid interactive windows in the canonical environment.
